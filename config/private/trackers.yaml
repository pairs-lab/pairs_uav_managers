pairs_uav_managers:

  control_manager:

    MpcTracker:
      address: "pairs_uav_trackers/MpcTracker"
      namespace: "mpc_tracker"
      human_switchable: true

    LandoffTracker:
      namespace: "landoff_tracker"
      address: "pairs_uav_trackers/LandoffTracker"

    LineTracker:
      namespace: "line_tracker"
      address: "pairs_uav_trackers/LineTracker"

    JoyTracker:
      namespace: "joy_tracker"
      address: "pairs_uav_trackers/JoyTracker"

    SpeedTracker:
      namespace: "speed_tracker"
      address: "pairs_uav_trackers/SpeedTracker"

    FlipTracker:
      namespace: "flip_tracker"
      address: "pairs_uav_trackers/FlipTracker"

    MidairActivationTracker:
      namespace: "midair_activation_tracker"
      address: "pairs_uav_trackers/MidairActivationTracker"

    NullTracker:
      namespace: "null_tracker"
      address: "pairs_uav_managers/NullTracker"
